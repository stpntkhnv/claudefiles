# dotnet-skills catalog — dotnet-advanced

- **csharp-scripts** — Run file-based C# apps with the .NET CLI when the user explicitly wants C#/.NET code without creating a project. Use for C# language/API experiments, one-file C# apps, small multi-file C# apps composed with `#:include`/`#:exclude`, or C# file-based apps linked with `#:ref`. Do not use for language-agnostic throwaway scripts, generic computations, Python/PowerShell-style automation, full projects, or existing app integration.
  `/home/stsiapan/devTools/skills/dotnet-skills/plugins/dotnet-advanced/skills/csharp-scripts/SKILL.md`
- **dotnet-pinvoke** — Correctly call native (C/C++) libraries from .NET using P/Invoke and LibraryImport. Covers function signatures, string marshalling, memory lifetime, SafeHandle, and cross-platform patterns. USE FOR: writing new P/Invoke or LibraryImport declarations, reviewing or debugging existing native interop code, wrapping a C or C++ library for use in .NET, diagnosing crashes, memory leaks, or corruption at the managed/native boundary. DO NOT USE FOR: COM interop, C++/CLI mixed-mode assemblies, or pure managed code with no native dependencies.
  `/home/stsiapan/devTools/skills/dotnet-skills/plugins/dotnet-advanced/skills/dotnet-pinvoke/SKILL.md`
- **nuget-trusted-publishing** — Set up NuGet trusted publishing (OIDC) on a GitHub Actions repo — replaces long-lived API keys with short-lived tokens. USE FOR: trusted publishing, NuGet OIDC, keyless NuGet publish, migrate from NuGet API key, NuGet/login, secure NuGet publishing. DO NOT USE FOR: publishing to private feeds or Azure Artifacts (OIDC is nuget.org only). INVOKES: shell (powershell or bash), edit, create, ask_user for guided repo setup.
  `/home/stsiapan/devTools/skills/dotnet-skills/plugins/dotnet-advanced/skills/nuget-trusted-publishing/SKILL.md`
