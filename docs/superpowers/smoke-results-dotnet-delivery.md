# Смоук-протокол: доставка dotnet-skills в superpowers

Дата прогона: 2026-07-04. Ссылки: план `docs/superpowers/plans/2026-07-04-dotnet-skills-delivery.md`, спека `docs/superpowers/specs/2026-07-04-dotnet-skills-delivery-design.md`.

Статус Task 4 (LSP-плагин): **SKIPPED** — на этой машине `dotnet` отсутствует в PATH (нет .NET SDK), диффа в devTools нет. Ядро схемы (роутер, каталог, план-транспорт, хук) от LSP не зависит и работает независимо. Инструкция для пользователя, если он захочет включить LSP-плагин:

1. Поставить .NET 10 SDK (`dotnet --version` должен показать `10.x`).
2. Установить плагин:
   ```bash
   claude plugin marketplace add dotnet/skills && claude plugin install dotnet@dotnet-agent-skills
   ```
   Если подкоманда `plugin` недоступна в CLI — интерактивные эквиваленты в REPL:
   ```
   /plugin marketplace add dotnet/skills
   /plugin install dotnet@dotnet-agent-skills
   ```
   и перезапустить сессию после установки.

---

## Step 1: Автопроверки артефактов (выполнено агентом)

Прогон 2026-07-04, из `/home/stsiapan/devTools`:

```
$ /home/stsiapan/devTools/skills/tools/test-gen-dotnet-catalog.sh
OK: 98 entries across 16 domain files, INDEX 4736 chars, total 84376 chars (~21094 tokens) -> /tmp/tmp.1X1rRXb0rj
PASS: all catalog tests

$ /home/stsiapan/devTools/skills/tools/test-detect-dotnet.sh
PASS: all hook tests

$ test -f /home/stsiapan/.claude/skills/dotnet-router/SKILL.md && echo "skill reachable"
skill reachable

$ test -f /home/stsiapan/.claude/skills/dotnet-router/INDEX.md && test -f /home/stsiapan/.claude/skills/dotnet-router/CATALOG-dotnet-test.md && echo "catalog reachable"
catalog reachable

$ python3 -m json.tool /home/stsiapan/.claude/settings.json > /dev/null && echo "settings valid"
settings valid
```

Результат: **все пять проверок PASS**, ожидание из брифа выполнено полностью. Дефектов не найдено.

Дополнительно проверено (не входило в чеклист, но relevant для доверия результату):
- Симлинк `~/.claude/skills/dotnet-router -> /home/stsiapan/devTools/claude/skills/dotnet-router` — цел, разрешается корректно (`readlink -f`).
- Хук зарегистрирован в `~/.claude/settings.json` как `SessionStart` → `command: /home/stsiapan/devTools/claude/hooks/detect-dotnet.sh`, файл хука исполняемый (`-rwxr-xr-x`).

---

## Step 2: Смоук мультисервисной детекции на реальном воркспейсе

**Статус: ожидает пользователя.** Корень реального рабочего воркспейса пользователя (директория, из которой обычно стартует Claude Code) не был доступен агенту в этой сессии — агент работал из `/home/stsiapan/devTools`, что не является тем воркспейсом, который нужно смоук-тестировать. Путь угадывать не стал.

Команда для пользователя (подставить реальный корень воркспейса вместо `<workspace-root>`):

```bash
time CLAUDE_PROJECT_DIR="<workspace-root>" /home/stsiapan/devTools/claude/hooks/detect-dotnet.sh
```

Ожидаемый результат: одна строка вида `.NET workspace detected (found <относительный путь до .sln/.csproj/global.json>). For any .NET work, invoke the dotnet-router skill first...`, и `real` из `time` — меньше 2 секунд. Фактическое время и найденный маркер нужно вписать сюда после прогона:

- Фактическая строка вывода: `_(заполнить)_`
- `real` время: `_(заполнить)_`

Если хук не сработал (пустой вывод в .NET-дереве) — см. Step 4, известный запасной ход: маркер лежит глубже `-maxdepth 6`, проверить `find <root> -maxdepth 8 -name '*.sln'` и при необходимости поднять `-maxdepth` в хуке (`claude/hooks/detect-dotnet.sh`) и в тесте (`skills/tools/test-detect-dotnet.sh`).

---

## Step 3: Поведенческий смоук (новая сессия из корня воркспейса)

**Статус: ожидает пользователя.** Выполняется в НОВОЙ сессии Claude Code, запущенной из корня рабочего воркспейса пользователя — агент не может провести живую сессию за пользователя. Чеклист ниже нужно пройти и отметить результат каждого пункта прямо в этом файле (галочка + короткая заметка о наблюдении).

## Поведенческий смоук (новая сессия из корня воркспейса)

1. [ ] Детекция: в начале сессии Claude знает о .NET-контексте
   (спросить: «это .NET-воркспейс?» — ответ должен ссылаться на детекцию,
   без поиска по файлам).
2. [ ] Автосрабатывание роутера: попросить «спланируй небольшую фичу в
   <сервис> на ASP.NET Core». Ожидание: Claude сам вызывает dotnet-router,
   читает INDEX.md и нужные CATALOG-<домен>.md, и в плане у каждой задачи
   есть блоки **Skills:** (абсолютные пути) и **Service root:**.
3. [ ] Делегирование: попросить выполнить одну задачу этого плана через
   субагента. Ожидание: в промпте субагента видны «Before starting, Read
   these skill files: …», «Work from: <service root>» и pull-строка с путём
   каталога.
4. [ ] LSP (только если Task 4 выполнена; при SKIPPED — пропустить):
   открыть вопрос по коду в двух РАЗНЫХ сервисах (например, «где определён
   класс X и кто его использует?» в сервисе A, затем в B).
   Ожидание: точная навигация без grep-перебора; сессия не деградирует
   по памяти (наблюдаемо: нет многоминутных зависаний warmup после первого).
   **Статус: пропущен — плагин не установлен** (Task 4 SKIPPED, нет .NET SDK на машине).
5. [ ] Не-.NET контроль: сессия из любой не-.NET директории — напоминание
   хука отсутствует.

Результаты пунктов 1, 2, 3, 5 — вписать сюда после прохождения пользователем (галочка + наблюдение); пункт 4 уже закрыт как пропущенный.

---

## Step 4: Дефекты

Автоматизируемая часть (Step 1) дефектов не выявила — все пять проверок PASS без отклонений.

Пункты, зависящие от пользователя (Step 2, Step 3), ещё не пройдены на момент записи этого протокола — соответственно, дефекты по ним пока не могут быть зафиксированы. Известные запасные ходы на случай провала (из брифа, не примененные — превентивно проблем не было):

- Симлинк не читается → fallback из Task 2 Step 4.
- LSP прожорлив на связанных сервисах → выключить плагин (`/plugin uninstall dotnet@dotnet-agent-skills`); остальная схема от него не зависит.
- Хук не сработал в реальном воркспейсе → проверить глубину залегания маркеров (`find <root> -maxdepth 8 -name '*.sln'`) и при необходимости поднять `-maxdepth` в хуке и тесте.

Если пользователь при прохождении Step 2/Step 3 обнаружит провал — дописать сюда отдельной записью: наблюдение, ожидание, подозреваемый компонент.

---

## Сводка

| Шаг | Статус | Кто выполняет |
|---|---|---|
| Step 1: автопроверки артефактов | PASS (все 5 проверок) | агент |
| Step 2: детекция на реальном воркспейсе | ожидает пользователя | пользователь |
| Step 3: поведенческий чеклист (сессия) | ожидает пользователя (п. 4 — пропущен, LSP SKIPPED) | пользователь |
| Step 4: дефекты | дефектов в автопроверках нет; по Step 2/3 — TBD | агент/пользователь |
| Task 4 (LSP-плагин) | SKIPPED (нет .NET SDK в PATH) | — (инструкция выше) |

## Follow-ups (из финального ревью ветки; применить при следующей регенерации каталога)

1. INDEX-указатели печатать каноническим путём `~/.claude/skills/dotnet-router/...` вместо `$OUTDIR` (сейчас оба пути работают через симлинк).
2. Дрейф-гард stage map: после цикла по плагинам генератор должен падать, если stage map ссылается на несуществующий домен; предупреждать о доменах вне карты.
3. Регекс теста 5c расширить до `[A-Za-z0-9._-]+` — цель гарда разделитель `, `, а не алфавит имён.
